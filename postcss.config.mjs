const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
  themes: {
    extend: {
      fontFamily: {
        sans: ["var(--font-inter)", "sans-serif"],
      },
    },
  },
};

export default config;
